import styled from 'styled-components';

const MainList = styled.ul`
  display: flex;
`;

const MainText = styled.div`
  font-size: 40px;
  font-style: bold;
  color: white;
`;

export {MainList, MainText};
